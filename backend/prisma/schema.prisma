generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id                    String @id
  name                  String
  email                 String
  password              String
  password_confirmation String

  @@map("users")
}
